{
  "name": "counsel-load-test",
  "version": "0.1.0",
  "private": true,
  "description": "Load testing scripts for Counsel API using Artillery",
  "scripts": {
    "test": "artillery run artillery.yml",
    "test:quick": "artillery run --output report.json artillery.yml",
    "report": "artillery report report.json"
  },
  "devDependencies": {
    "artillery": "^2.0.20"
  }
}
